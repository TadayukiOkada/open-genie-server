# セキュリティポリシー

*[English](./SECURITY.md) | 日本語*

## このサーバが何であるか、そしてそれが何を意味するか

open-genie-server は、Qualcomm Genie C API と量子化済みモデルバンドルのための
**計測器**です([このプロジェクトの目的](./README.ja.md#このプロジェクトの目的))。
**本番用のサービング基盤ではなく**、本番サーバなら当然備えているはずのものを
いくつか**意図的に持っていません**。以下はいずれも設計上の決定であり、そのように
文書化してあります。これらを脆弱性として報告いただいても、**README の冒頭に書いてある以上の
情報にはなりません**:

- **認証もレート制限もありません。** ポートに到達できる者に対して、全エンドポイントが開いています。
- **`POST /v1/models/switch` は、サーバプロセスが読めるあらゆるパスを開きます。**
  `MODELS_BASE_DIR` はパスを短く書くためのもので、閉じ込めではありません。
  許可リスト方式は検討したうえで採用していません —
  [モデルパスの解決ルール](./docs/MANUAL.ja.md#モデルパスの解決ルール)を参照。
- **1プロセス・スロットごとに1ロック・マルチプロセスのスケーリングなし。**
  遅いリクエストがそのスロットを塞ぐのは設計どおりです。
- **QAIRT SDK の欠陥は本サーバが直すものではありません。** 特に素のライブラリによる
  スロット恒久故障は、回避せずに**報告する**方針です。バージョン別の一覧は
  [QAIRT バージョン別の問題点](./docs/QAIRT_VERSIONS.ja.md)にあり、`libGenie.so` の欠陥は
  Qualcomm に属します。

**自分で管理しているネットワークで動かしてください。** それ以外の場所から到達させる必要があるなら、
**前段に認証とアクセス制御を置いてください**(リバースプロキシが通常の答えです)。
また、**モデルディレクトリは信頼できる入力として扱ってください**。

## 報告してほしいもの

**上に挙げた権限を超えて**何かができてしまうもの、すべてです:

- 文書化されたどの機能でも開かないはずのパスの読み書き、あるいは `/v1/models/switch`
  以外のエンドポイント経由で、プロセス自身のモデル・キャッシュディレクトリの外に届いてしまうもの。
- **正常な形式のリクエストから到達できる**メモリ破壊・クラッシュ・ハング。ctypes 層は
  C API にバッファを渡しているので、**長さや生存期間の誤りは機能の不足ではなく本物のバグ**です。
- **あるリクエストのデータが別のリクエストに漏れる**もの: モデルや LoRA アダプタをまたぐ
  prefix キャッシュの名前空間、スロットのルーティング、ストリーミングのフィルタなど。
- **実際に動くコードに届く**依存パッケージの脆弱性情報。

報告には、**引き金になったリクエスト**、`env_config.json` の形(パスは伏せて構いません)、
**QAIRT のバージョン**があると大幅に扱いやすくなります。オフラインテストスイートは NPU 無しで
動くので、デバイスが必須でない欠陥であれば
[`tests/fake_genie.py`](./tests/fake_genie.py) に対する再現手順が理想的です。

## 報告方法

このリポジトリの GitHub の**非公開の脆弱性報告**を使ってください:
**Security → Report a vulnerability**。調査中は公開されません。

**機微でないもの**は通常の Issue を使ってください — 上に挙げた設計上の決定について、
「こちらが想定していない用途では間違っている」というご意見も含みます。
その議論は公開の場にある方が有用です。

## サポート対象のバージョン

開発は `master` で行い、修正もそこに入ります。**長期サポートブランチも、過去のタグへの
バックポートもありません** — 古いバージョンで動かしている場合、答えは「現行版に上げてください」に
なります。報告時には、**問題を見たバージョン**(`genie-server --version`、または
`genie_server.__version__`)を書いてください。どのコミットが入っているかが分かります。
